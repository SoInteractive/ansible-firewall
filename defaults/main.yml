---

firewall_enable_dns: True
firewall_enable_ntp: True

#firewall_env_subnet: "{{ ansible_default_ipv4.network }}/24"
firewall_env_subnet: ""

firewall_flush: false
firewall_allow:
#  - { source: "91.227.212.0/22", port: "80" }
#  - { source: "91.227.212.0/22", port: "443" }
#  - { source: "192.168.14.0/23" }
  - { interface: "docker0" }
#  - { interface: "eth1" }

firewall_provided_interfaces: "{{ firewall_allow | map(attribute='interface') | select('defined') | reject('search','docker') | list }}"
firewall_available_interfaces: "{{ ansible_interfaces | list }}"

